"use strict";
var React = require("react");

module.exports = React.createClass({
  render: function () {
    return (
      React.DOM.div(null,
        React.DOM.p(null, this.props.name),
        React.DOM.p(null, this.props.rarity)
      )
    );
  }
});
